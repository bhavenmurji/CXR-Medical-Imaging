# Minimal requirements for CXR dataset downloading and processing
# These packages have pre-built wheels and don't require compilation

# DICOM and Medical Imaging (lightweight)
pydicom>=2.4.0
nibabel>=5.0.0
Pillow>=10.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Dataset Download Tools
kaggle>=1.5.0
gdown>=4.7.0
requests>=2.31.0
tqdm>=4.66.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Optional: Visualization (if needed later)
# matplotlib>=3.7.0

# Optional: Advanced imaging (skip for now - requires compilation)
# SimpleITK
# opencv-python-headless
# scikit-image
# scipy
